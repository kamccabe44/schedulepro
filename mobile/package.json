{
  "name": "schedulepro-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor wrapper that packages the SchedulePro web app as native iOS and Android apps.",
  "scripts": {
    "sync-web": "bash scripts/sync-web.sh",
    "prepare:ios": "npm run sync-web && npx cap add ios && npx cap sync ios",
    "prepare:android": "npm run sync-web && npx cap add android && npx cap sync android",
    "sync": "npm run sync-web && npx cap sync",
    "open:ios": "npx cap open ios",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/app": "^6.0.1",
    "@capacitor/browser": "^6.0.3",
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2",
    "@capacitor/push-notifications": "^6.0.2",
    "@capacitor/share": "^6.0.2",
    "@capacitor/status-bar": "^6.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
